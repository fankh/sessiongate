"use strict";
document.querySelector("#login").addEventListener("submit",async event=>{event.preventDefault();const status=document.querySelector("#status"),button=event.currentTarget.querySelector("button"),data=Object.fromEntries(new FormData(event.currentTarget));status.textContent="";button.disabled=true;try{const response=await fetch("/api/v1/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!response.ok)throw new Error(await response.text()||"Login failed");const login=await response.json();sessionStorage.setItem("csrf_token",login.csrf_token);location.assign(login.roles.includes("administrator")?"/admin.html":"/")}catch(error){status.textContent=error.message;button.disabled=false}});
